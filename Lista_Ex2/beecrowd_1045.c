#include <stdio.h>
 
int main() {
 
double A, B, C;

    scanf("%lf %lf %lf", &A, &B, &C);

    if (A < B || A < C ) {
        A == B || A == C && A == B || A == C;

    }

    printf("%lf %lf %lf", A, B, C);
    
 
    return 0;
}